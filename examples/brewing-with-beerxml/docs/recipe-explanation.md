# Recipe Explanation

This example shows how a BeerXML recipe can be retained inside an OKPF package while normalized OKPF records provide broader knowledge-package context.

OKPF does not replace BeerXML. It wraps, references, or augments domain-specific files with manifest metadata, provenance, licensing, attribution, and AI/tooling guidance.
