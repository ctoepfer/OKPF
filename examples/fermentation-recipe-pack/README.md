# Fermentation Recipe Pack Example

This example shows recipe records using the optional OKPF fermentation profile. The records are synthetic and are intended to demonstrate structure, facets, and profile-aware validation.

The profile is useful to Lumina-like consumers, but the package does not require Lumina and does not include Lumina-specific fields.

