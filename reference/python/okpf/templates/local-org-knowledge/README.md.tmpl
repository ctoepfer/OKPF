<!-- SPDX-License-Identifier: Apache-2.0 -->

# {{ name }}

Generated by `okpf init --template local-org-knowledge`.

- `artifacts/policy.md` — the human-readable policy or process document.
- `records/knowledge.jsonl` — the same knowledge as structured OKPF records.

Defaults to a non-commercial, internal-use license — adjust
`license`/`usage_policy` in `manifest.json` before sharing outside your
organization. Run `okpf validate .` before sharing the pack.
